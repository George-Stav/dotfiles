# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!
# class                 border  backgr. text    indicator child_border
# client.focused          #4c7899 #285577 #ffffff #2e9ef4   #285577
# client.focused_background		#DFC2E8 #CB9DDA #cccccc #B778CC   #CB9DDA

# set $mod Mod4 # windows
set $mod Mod1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

focus_follows_mouse no
mouse_warping none
hide_edge_borders smart_no_gaps
default_border pixel 1
default_floating_border normal

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
# font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

font pango:Iosevka 10

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# kill focused window
bindsym $mod+q kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# bindsym Next focus right
# bindsym Prior focus left

# move focused window; 100 px is only relevant for floating windows
bindsym $mod+Ctrl+h move left 100 px
bindsym $mod+Ctrl+j move down 100 px
bindsym $mod+Ctrl+k move up 100 px
bindsym $mod+Ctrl+l move right 100 px

# enter fullscreen mode for the focused container
bindsym $mod+Shift+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Ctrl+f floating toggle
bindsym $mod+f focus mode_toggle

# switch to workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10
bindsym $mod+minus workspace 11

# move focused container to workspace
bindsym $mod+Ctrl+1 move container to workspace 1
bindsym $mod+Ctrl+2 move container to workspace 2
bindsym $mod+Ctrl+3 move container to workspace 3
bindsym $mod+Ctrl+4 move container to workspace 4
bindsym $mod+Ctrl+5 move container to workspace 5
bindsym $mod+Ctrl+6 move container to workspace 6
bindsym $mod+Ctrl+7 move container to workspace 7
bindsym $mod+Ctrl+8 move container to workspace 8
bindsym $mod+Ctrl+9 move container to workspace 9
bindsym $mod+Ctrl+0 move container to workspace 10
bindsym $mod+Ctrl+minus move container to workspace 11

# Switch workspaces on outputs (left -> right & right -> left)
bindsym $mod+Ctrl+grave mark current; focus output next; move workspace to output next; [con_mark="current"] focus; move workspace to output next; unmark

# Workspace manipulation
bindsym $mod+grave workspace back_and_forth
bindsym $mod+comma focus output left
bindsym $mod+period focus output right
bindsym $mod+Ctrl+comma move workspace to output left
bindsym $mod+Ctrl+period move workspace to output left
bindsym $mod+Shift+comma workspace prev_on_output
bindsym $mod+Shift+period workspace next_on_output

# Layouts
bindsym $mod+Escape layout toggle tabbed splitv splith
bindsym $mod+Shift+Escape layout toggle splith splitv tabbed 

# Launcher
bindsym $mod+Return exec alacritty
bindsym $mod+m exec --no-startup-id "rofi -m -4 -modi drun -show drun"
bindsym $mod+space exec --no-startup-id "rofi -m -4 -show window"
bindsym $mod+Shift+s exec "flameshot gui"
bindsym $mod+b exec "firefox-developer-edition"
bindsym $mod+c exec "google-chrome-stable"
bindsym $mod+a exec "$HOME/.local/bin/run"
bindsym $mod+t exec "$HOME/.local/bin/thunar-goto"

# Emacs
bindsym $mod+e exec "$HOME/.local/bin/em n"
bindsym $mod+Shift+e exec "emacs"

# Volume
bindsym Shift+XF86AudioLowerVolume exec  "$HOME/.local/bin/change-volume -10% && pkill -SIGRTMIN+13 i3blocks"
bindsym XF86AudioLowerVolume exec  "$HOME/.local/bin/change-volume -5% && pkill -SIGRTMIN+13 i3blocks"
bindsym XF86AudioRaiseVolume exec "$HOME/.local/bin/change-volume +5% && pkill -SIGRTMIN+13 i3blocks"
bindsym Shift+XF86AudioRaiseVolume exec "$HOME/.local/bin/change-volume +10% && pkill -SIGRTMIN+13 i3blocks"
bindsym XF86AudioMute exec "$HOME/.local/bin/change-volume 0 && pkill -SIGRTMIN+13 i3blocks"
bindsym Shift+XF86AudioMute exec "$HOME/.local/bin/toggle-mic && pkill -SIGRTMIN+10 i3blocks"

# Duplicates of the above for Blackwidow keyboard
bindsym Shift+F2 exec  "$HOME/.local/bin/change-volume -10% && pkill -SIGRTMIN+13 i3blocks"
bindsym F2 exec  "$HOME/.local/bin/change-volume -5% && pkill -SIGRTMIN+13 i3blocks"
bindsym F3 exec "$HOME/.local/bin/change-volume +5% && pkill -SIGRTMIN+13 i3blocks"
bindsym Shift+F3 exec "$HOME/.local/bin/change-volume +10% && pkill -SIGRTMIN+13 i3blocks"
bindsym F1 exec "$HOME/.local/bin/change-volume 0 && pkill -SIGRTMIN+13 i3blocks"
bindsym Shift+F1 exec "$HOME/.local/bin/toggle-mic && pkill -SIGRTMIN+10 i3blocks"

# Player
bindsym XF86AudioPlay exec "playerctl play-pause && $HOME/.local/bin/media-notif"
bindsym Shift+XF86AudioPlay exec "playerctl -p spotify play-pause && $HOME/.local/bin/media-notif spotify"
bindsym XF86AudioNext exec "playerctl next"
bindsym XF86AudioPrev exec "playerctl previous"
bindsym Shift+XF86AudioNext exec "pkill -SIGRTMIN+12 i3blocks"
bindsym Shift+XF86AudioPrev exec "pkill -SIGRTMIN+12 i3blocks"

# Duplicates of the above for Blackwidow keyboard
bindsym F6 exec "playerctl play-pause && $HOME/.local/bin/media-notif"
bindsym Shift+F6 exec "playerctl -p spotify play-pause && $HOME/.local/bin/media-notif spotify"
bindsym F5 exec "playerctl next"
bindsym F7 exec "playerctl previous"
bindsym Shift+F5 exec "pkill -SIGRTMIN+12 i3blocks"
bindsym Shift+F7 exec "pkill -SIGRTMIN+12 i3blocks"

# Brightness
bindsym XF86MonBrightnessDown exec "brightnessctl set 5%-"
bindsym XF86MonBrightnessUp exec "brightnessctl set +5%"

# Scripts
bindsym $mod+Ctrl+space exec "$HOME/.local/bin/cycle-kb-layout"
bindsym $mod+v exec "$HOME/.local/bin/vpn toggle && pkill -SIGRTMIN+11 i3blocks"

# reload the configuration file
bindsym $mod+Ctrl+c reload
bindsym $mod+Ctrl+r restart
bindsym $mod+Ctrl+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
# bindsym $mod+Ctrl+q exit

bindsym $mod+Ctrl+Shift+s exec "systemctl suspend"
bindsym $mod+Ctrl+Shift+h exec "systemctl hibernate"
bindsym $mod+Ctrl+Shift+r exec "$HOME/.local/bin/em p state && reboot"
bindsym $mod+Ctrl+Shift+u exec "$HOME/.local/bin/em p state && shutdown now"

# px for floating | ppt for tiled
bindsym $mod+Shift+h resize shrink width 100 px or 10 ppt
bindsym $mod+Shift+j resize grow height 100 px or 10 ppt
bindsym $mod+Shift+k resize shrink height 100 px or 10 ppt
bindsym $mod+Shift+l resize grow width 100 px or 10 ppt

# Assign
assign [class="Virt-manager|jetbrains-studio"] number 5
assign [class="qBittorrent"] number 6
assign [class="pentablet"] number 6
assign [class="steam"] number 7
assign [class="discord"] number 7
# assign [class="Google-chrome"] number 8
assign [class="Spotify"] number 9
assign [class="steam_app.*"] number 11
assign [class="Risk of Rain 2"] number 11
assign [title="osu!"] number 11
assign [class="EXAPUNKS"] number 11
assign [title="Furi"] number 11
assign [class="WizardOfLegend.x86_64"] number 11
assign [class="Minecraft.*"] number 11
assign [class="Stardew Valley"] number 11


bindsym $mod+Ctrl+equal move scratchpad
bindsym $mod+equal scratchpad show

no_focus [window_role="pop_up"]

# for_window directives

for_window [window_role="browser"] layout tabbed
for_window [window_role="Emacs"] layout tabbed

for_window [class="Thunar"] floating enable
for_window [class="genp"] floating enable
for_window [class="pentablet"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [title=".*QEMU/KVM"] floating enable
for_window [title="Steam Settings"] floating enable
for_window [class="anki"] floating enable
for_window [class="Emulator"] floating enable
for_window [class="pavucontrol"] floating enable

# for_window [class=".*_floating"] floating enable
for_window [class="Alacritty_floating|Galculator"] floating enable
for_window [class="Alacritty_floating|Galculator"] move container to output next
for_window [class="Alacritty_floating|Galculator"] sticky enable

bindsym $mod+Ctrl+Escape mode "no_bindings (exit: Ctrl+Esc)"
mode "no_bindings (exit: Ctrl+Esc)" {
    # back to normal: Ctrl+Escape
    bindsym Ctrl+Escape mode "default"
}

# Not required, just kept for the purpose of example
# bindsym $mod+p mode "move_float" focus floating
mode "move_float" {
    bindsym Left  move left 100 px
    bindsym Down  move down 100 px
    bindsym Up    move up 100 px
    bindsym Right move right 100 px

    bindsym h     move left 100 px
    bindsym j     move down 100 px
    bindsym k     move up 100 px
    bindsym l     move right 100 px

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym p mode "default"
}

# resize window (you can also use the mouse for that)
mode "resize" {
	# These bindings trigger as soon as you enter the resize mode

	# Pressing left will shrink the window’s width.
	# Pressing right will grow the window’s width.
	# Pressing up will shrink the window’s height.
	# Pressing down will grow the window’s height.
	bindsym j resize shrink width 10 px or 10 ppt
	bindsym k resize grow height 10 px or 10 ppt
	bindsym l resize shrink height 10 px or 10 ppt
	bindsym semicolon resize grow width 10 px or 10 ppt

	# same bindings, but for the arrow keys
	bindsym Left resize shrink width 10 px or 10 ppt
	bindsym Down resize grow height 10 px or 10 ppt
	bindsym Up resize shrink height 10 px or 10 ppt
	bindsym Right resize grow width 10 px or 10 ppt

	# back to normal: Enter or Escape
	bindsym Return mode "default"
	bindsym Escape mode "default"
}

# bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	# status_command i3status --config ~/.config/i3/i3status.conf
	# output primary
	status_command i3blocks -c ~/.config/i3/i3blocks.conf
}

# exec --no-startup-id "$HOME/.local/bin/smg r"

# bar {
# 	output nonprimary
#         status_command i3blocks -c ~/.config/i3/i3blocks.conf
#         # status_command i3blocks -c ~/.config/i3/i3blocks-simple.conf
# }
